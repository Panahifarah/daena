#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if command -v systemctl >/dev/null 2>&1; then
            systemctl stop daena.service >/dev/null 2>&1 || true
            systemctl disable daena.service >/dev/null 2>&1 || true
        fi
        ;;
esac
